
server.port=8082



## Spring DATASOURCE (DataSourceAutoConfiguration & DataSourceProperties)
spring.datasource.url=jdbc:mysql://localhost:3306/CMU?createDatabaseIfNotExist=true
#spring.datasource.url=jdbc:mysql://localhost:3306/generic?createDatabaseIfNotExist=true
spring.datasource.username=kaushik
spring.datasource.password=kaushik

#spring.datasource.url = jdbc:mysql://localhost:3306/generic?useSSL=false&serverTimezone=UTC&useLegacyDatetimeCode=false


spring.jpa.hibernate.use-new-id-generator-mappings=false


# ===============================
# = JPA / HIBERNATE
# ===============================

# Use spring.jpa.properties.* for Hibernate native properties (the prefix is
# stripped before adding them to the entity manager).

# Show or not log for each sql query
spring.jpa.show-sql = true

# Hibernate ddl auto (create, create-drop, update): with "update" the database
# schema will be automatically updated accordingly to java entities found in
# the project
spring.jpa.hibernate.ddl-auto = update
